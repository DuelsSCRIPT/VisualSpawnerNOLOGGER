# Leaked at Vexhub https://discord.gg/Yukcuyz6h6

Free notfiy is here
https://discord.gg/H6JpjjwXzx

local Players = game:GetService("Players")

local TeleportService = game:GetService("TeleportService")

local HttpService = game:GetService("HttpService")

local UIS = game:GetService("UserInputService")

local player = Players.LocalPlayer

local AutoJoinEnabled = false

local ScreenGui = Instance.new("ScreenGui")

ScreenGui.Name = "EternalHub"

ScreenGui.Parent = player:WaitForChild("PlayerGui")

ScreenGui.ResetOnSpawn = false

local Main = Instance.new("Frame")

Main.Size = UDim2.new(0, 240, 0, 130)

Main.Position = UDim2.new(1, -260, 0, 70) -- en haut à droite, ajustable

Main.BackgroundColor3 = Color3.fromRGB(15, 15, 25)

Main.Parent = ScreenGui

Main.Active = true

Main.Draggable = true

local UICorner = Instance.new("UICorner", Main)

UICorner.CornerRadius = UDim.new(0, 14)

local UIStroke = Instance.new("UIStroke", Main)

UIStroke.Thickness = 2

UIStroke.Color = Color3.fromRGB(150, 120, 255)

local UIGradient = Instance.new("UIGradient", Main)

UIGradient.Color = ColorSequence.new{

	ColorSequenceKeypoint.new(0, Color3.fromRGB(90, 0, 140)),	ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0, 120, 255)),

	ColorSequenceKeypoint.new(1, Color3.fromRGB(170, 0, 255))

}

UIGradient.Rotation = 40

local Title = Instance.new("TextLabel")

Title.Size = UDim2.new(1, -20, 0, 36)

Title.Position = UDim2.new(0, 10, 0, 6)

Title.BackgroundTransparency = 1

Title.Text = "Eternal hub 🌌"

Title.Font = Enum.Font.GothamBold

Title.TextSize = 22

Title.TextXAlignment = Enum.TextXAlignment.Left

Title.TextColor3 = Color3.fromRGB(255,255,255)

Title.Parent = Main

local TitleGradient = Instance.new("UIGradient", Title)

TitleGradient.Color = ColorSequence.new{

	ColorSequenceKeypoint.new(0, Color3.fromRGB(200, 160, 255)),

	ColorSequenceKeypoint.new(1, Color3.fromRGB(120, 200, 255))

}

local AutoJoinBtn = Instance.new("TextButton")

AutoJoinBtn.Size = UDim2.new(0.85, 0, 0, 42)

AutoJoinBtn.Position = UDim2.new(0.075, 0, 0.48, 0)

AutoJoinBtn.Text = "Auto Joiner : OFF"

AutoJoinBtn.Font = Enum.Font.GothamBold

AutoJoinBtn.TextScaled = true

AutoJoinBtn.TextColor3 = Color3.fromRGB(255,255,255)

AutoJoinBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 70)

AutoJoinBtn.Parent = Main

local BtnCorner = Instance.new("UICorner", AutoJoinBtn)

BtnCorner.CornerRadius = UDim.new(0, 10)

local BtnStroke = Instance.new("UIStroke", AutoJoinBtn)

BtnStroke.Thickness = 2

BtnStroke.Color = Color3.fromRGB(120, 120, 255)

-- Fonction AutoJoin

local function AutoJoin()

	local PLACE_ID = 109983668079237

	local servers = {}

	pcall(function()

		local url = "https://games.roblox.com/v1/games/"..PLACE_ID.."/servers/Public?sortOrder=Asc&limit=100"

		local response = game:HttpGet(url)

		local data = HttpService:JSONDecode(response)

		if data and data.data then

			for _,v in pairs(data.data) do

				if v.playing < v.maxPlayers then

					table.insert(servers, v.id)

				end

			end

		end

	end)

	if #servers > 0 then

		TeleportService:TeleportToPlaceInstance(

			PLACE_ID,

			servers[math.random(1,#servers)],

			player

		)

	end

end

-- Bouton ON/OFF

AutoJoinBtn.MouseButton1Click:Connect(function()

	AutoJoinEnabled = not AutoJoinEnabled

	if AutoJoinEnabled then

		AutoJoinBtn.Text = "Auto Joiner : ON"

		AutoJoinBtn.BackgroundColor3 = Color3.fromRGB(60, 120, 255)

		BtnStroke.Color = Color3.fromRGB(170, 200, 255)

		task.spawn(AutoJoin)

	else

		AutoJoinBtn.Text = "Auto Joiner : OFF"

		AutoJoinBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 70)

		BtnStroke.Color = Color3.fromRGB(120, 120, 255)

	end

end)
